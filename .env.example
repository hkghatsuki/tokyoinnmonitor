# 必填：要查詢的地區 ID（可多項，逗號分隔）
AREA_IDS=475,476

# 相容舊版：若沒填 AREA_IDS，會讀取 AREA_ID（單一）
# AREA_ID=475

# 必填：入住日期。支援 YYYY-MM-DD 或完整 ISO8601（UTC）
CHECKIN_DATE=2026-04-03

# 選填：退房日期。未填時預設為入住 +1 天
CHECKOUT_DATE=2026-04-04

# 選填：查詢參數
NUMBER_OF_PEOPLE=2
NUMBER_OF_ROOM=1
SMOKING_TYPE=all

# 選填：若要限制只查某些飯店再填；預設為各地區全部飯店
HOTEL_CODES=

# 選填：狀態檔案位置（用於去重）
STATE_FILE=.toyoko_state.json

# 選填：第一次執行是否因「狀態變化」而通知（true/false）
NOTIFY_ON_FIRST_RUN=false

# 選填：只要有任一飯店有空房就每次通知（true/false）
NOTIFY_WHEN_AVAILABLE_ALWAYS=true

# 避免被判定為 bot 的單次查詢節流設定（秒）
MIN_REQUEST_INTERVAL_SECONDS=1.5
REQUEST_JITTER_SECONDS=1.2
AREA_LOOP_DELAY_SECONDS=2.0

# 內建排程：每個查詢循環的間隔（秒）
SCHEDULE_INTERVAL_SECONDS=900
SCHEDULE_JITTER_SECONDS=30

# 若只想測一次，設 true；正式常駐請設 false
RUN_ONCE=false

# Telegram 通知（選填，兩者都要有才會送）
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# LINE Bot 通知（選填，兩者都要有才會送）
# Channel access token（Messaging API）
LINE_BOT_CHANNEL_ACCESS_TOKEN=
# 推播目標 ID（userId / groupId / roomId）
LINE_BOT_TO=
